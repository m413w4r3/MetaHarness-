{
  "name": "nimbalyst-metaharness",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Nimbalyst extension for inspecting MetaHarness runs.",
  "scripts": {
    "build": "vite build && vite build --config vite.backend.config.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --input-type=module -e \"import { readFileSync } from 'node:fs'; import { validateExtensionBundle } from './node_modules/@nimbalyst/extension-sdk/dist/validate.js'; const manifest = JSON.parse(readFileSync('manifest.json', 'utf8')); if (manifest.id !== 'com.m413w4r3.metaharness') throw new Error('Unexpected extension id'); const result = await validateExtensionBundle('./dist'); if (!result.valid) { console.error(result.errors.join('\\n')); process.exit(1); } if (result.warnings.length) console.warn(result.warnings.join('\\n'));\""
  },
  "devDependencies": {
    "@nimbalyst/extension-sdk": "0.6.0",
    "@types/node": "^20.0.0",
    "@types/react": "^19.2.17",
    "@vitejs/plugin-react": "^5.1.2",
    "react": "^19.2.7",
    "typescript": "^5.0.0",
    "vite": "^7.1.12"
  }
}
