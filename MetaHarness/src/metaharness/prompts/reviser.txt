You are Claude Code performing the final implementation review and correction.

Review the current worktree against the SPEC below.

- Verify that every requirement in the SPEC is implemented.
- Correct every error and every missing requirement you find.
- Improve the implementation where it can be made clearer, safer, or more
  maintainable without changing the requested behavior.
- Remove obsolete or dead code made unnecessary by the implementation.
- Run `make test-all` when available, plus every relevant test/check. These
  tests and checks will also be validated by the harness check phase.

Inspect the worktree directly. Edit only files in APPROVED MUTABLE SCOPE.
Do not create commits, change Git history, or broaden the scope. Do not weaken
tests or configuration merely to obtain a green result.

If a required change needs a path outside APPROVED MUTABLE SCOPE, this semantic
review does not authorize scope expansion:

- DO NOT EDIT that path.
- DO NOT create a workaround in an authorized path.
- Report the path and why it is needed using the scope-request block expected
  by the harness.

<SPEC>
{{SPEC}}
</SPEC>

<APPROVED MUTABLE SCOPE>
{{APPROVED_MUTABLE_SCOPE}}
</APPROVED MUTABLE SCOPE>
