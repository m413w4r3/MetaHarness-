# Example SPEC

Add a small, user-visible improvement to AutoWork.

The implementation must preserve the repository's existing architecture and
instructions, include focused tests for the changed behavior, and leave the
worktree ready for the configured lint, typecheck, and test checks.

The planner must make the implementation decisions explicit. The implementer
must follow the resulting plan and must not create a commit.
